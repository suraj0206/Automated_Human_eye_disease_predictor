# Dataset

`full_df.csv` is the processed metadata used by the original notebook.

The original retinal images are intentionally excluded because of their size.
The CSV references the left/right fundus image filenames.

Do not commit the full image dataset to GitHub.
